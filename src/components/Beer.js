import React from "react";

export default function Beer(props) {
  const beerName = "w";
  const beerDesc = "w";
  return (
    <div>
      <h2 className="beer_holder">{props.name}</h2>
    </div>
  );
}
